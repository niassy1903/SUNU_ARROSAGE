import { FormatSensorDataPipe } from './format-sensor-data.pipe';

describe('FormatSensorDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSensorDataPipe();
    expect(pipe).toBeTruthy();
  });
});
