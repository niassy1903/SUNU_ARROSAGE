import { TimeCountdownPipePipe } from './time-countdown.pipe.pipe';

describe('TimeCountdownPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeCountdownPipePipe();
    expect(pipe).toBeTruthy();
  });
});
